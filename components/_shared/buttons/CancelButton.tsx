import React from 'react'
import { SecondaryButton } from '.'

function CancelButton() {
    return (
        <SecondaryButton>
            Cancel
        </SecondaryButton>
    )
}

export default CancelButton
